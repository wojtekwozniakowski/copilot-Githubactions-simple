{
  "name": "github-actions-webapp-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PoC web app showcasing GitHub Actions CI/CD capabilities",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "lint": "eslint .",
    "test": "vitest run --coverage",
    "format:check": "prettier --check .",
    "format": "prettier --write .",
    "build:metadata": "node scripts/generate-build-metadata.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@vitest/coverage-v8": "^4.0.18",
    "eslint": "^10.0.1",
    "eslint-config-prettier": "^10.0.2",
    "prettier": "^3.5.1",
    "vite": "^6.2.0",
    "vitest": "^4.0.18"
  }
}
